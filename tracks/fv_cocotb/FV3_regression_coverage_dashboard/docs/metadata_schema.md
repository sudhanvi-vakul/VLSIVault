# metadata.json schema (v1)

- dut: string
- test: string
- seed: int
- txns: int
- cmd: string (exact replay command)
- tool_versions: object
- timestamp_utc: string
- checkpoint_sha256: string (zip hash, because this track is 'no git')
